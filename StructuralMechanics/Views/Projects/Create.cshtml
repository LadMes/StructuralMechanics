@model CreateProjectViewModel

@{
    ViewBag.Title = "Create Project";
}

<form>
    <div class="form-row">
        <div asp-validation-summary="All" class="error-message"></div>
    </div>
    <input hidden asp-for="ApplicationUser.Id"/>
    <div class="form-row">
        <div class="form-item">
            <label asp-for="ProjectName"></label>
            <input asp-for="ProjectName" />
            <span asp-validation-for="ProjectName" class="error-message"></span>
        </div>
        <div class="form-item">
            <label asp-for="Structure.StructureType"></label>
            <select id="structureTypes" asp-for="Structure.StructureType" asp-items="Html.GetEnumSelectList<StructureType>()" onChange="structureTypeChanged()">
                <option value="">Please select structure type</option>
            </select>
            <span asp-validation-for="Structure.StructureType" class="error-message"></span>
        </div>
    </div>
    <div id="thinWalledStructure" class="form-row" style="display: none;">
        <div class="form-item">
            <label asp-for="ThinWalledStructure.ThinWalledStructureType"></label>
            <select id="thinWalledStructureTypes" asp-for="ThinWalledStructure.ThinWalledStructureType" asp-items="Html.GetEnumSelectList<ThinWalledStructureType>()">
                <option value="">Please select thin-walled type</option>
            </select>
            <span asp-validation-for="ThinWalledStructure.ThinWalledStructureType" class="error-message"></span>
        </div>
    </div>
    <hr class="main-hr" />
    <button type="submit" class="btn btn-primary">Create</button>
</form>

@section Scripts {
    <script type="text/javascript">
        function structureTypeChanged() {
            var select = document.getElementById("structureTypes");
            if (select.options[select.selectedIndex].value == @((int)StructureType.ThinWalledStructure)) {
                document.getElementById("thinWalledStructure").style.display = "flex";  
            }
            else {
                alert();
            }
        } 
    </script>
}